# Simple Table 功能待办

本文档记录 Simple Table 相对于日常电子表格工作流仍需补齐的产品能力。

当前基础能力包括 XLSX、XLSM 和 CSV 打开与保存、单元格和公式编辑、
搜索、撤销与重做、行列及工作表结构操作、图片操作、脏状态、虚拟滚动，
以及 Web 和移动端恢复。以下事项主要是功能扩展和体验完善，不表示现有
基础编辑闭环不可用。

## P0：核心表格交互

- [ ] 实现多单元格范围选择。
  - 支持鼠标或触摸拖选矩形区域。
  - 支持 Shift 扩展选区。
  - 支持选择整行、整列和全表。
  - 正确处理合并单元格和虚拟滚动区域。
- [ ] 实现复制、剪切和粘贴。
  - 支持系统剪贴板和 TSV 表格数据。
  - 支持区域粘贴、跨工作表复制和批量撤销。
  - 对超出资源限制或受保护工作簿给出明确提示。
- [ ] 补齐键盘操作。
  - 支持方向键、Tab、Shift+Tab、Home、End 和 PageUp/PageDown 导航。
  - 支持 Delete/Backspace 清空选区。
  - 支持 Ctrl/Cmd+C、X、V、Z、Shift+Z、F 等常用快捷键。
  - 区分选中状态和编辑状态，避免单击后强制进入编辑。
- [ ] 渲染并编辑单元格样式。
  - 接入现有区域协议中的 `cell_formats` 和 `cell_styles`。
  - 渲染字体颜色、背景色、粗体、斜体和水平/垂直对齐。
  - 提供数字格式、字体、填充、边框和对齐工具。
  - 样式修改必须参与脏状态、撤销、重做和 XLSX 保存。
- [x] 实现基础排序与筛选。
  - [x] 支持单列升序和降序。
  - [x] 支持表头筛选和清除筛选。
  - [x] 明确排序范围、空值、公式和合并单元格的处理规则。
  - [x] 操作通过引擎协议执行并支持撤销和重做。

## P1：编辑能力完善

- [ ] 完善工作表管理。
  - 支持重命名、复制和拖动排序。
  - 支持隐藏和取消隐藏工作表。
  - 删除工作表前提供确认或清晰的可撤销反馈。
- [ ] 完善行列交互。
  - 支持点击表头选择整行或整列。
  - 提供表头上下文菜单。
  - 支持拖拽调整尺寸和自动适应内容。
  - 支持隐藏和取消隐藏行列以及批量结构操作。
- [ ] 支持创建和取消合并单元格。
  - 定义合并时非锚点内容的处理规则。
  - 保持公式引用、结构编辑和撤销重做行为一致。
- [ ] 增加查找与替换。
  - 支持替换当前项和全部替换。
  - 支持大小写、精确匹配以及当前工作表/全部工作表范围。
  - 支持按公式源文本或显示值搜索。
- [ ] 改善公式编辑体验。
  - 提供函数自动补全、参数提示和函数选择入口。
  - 为引用范围着色并支持点击或拖选插入引用。
  - 允许从公式诊断项直接跳转到问题单元格。
  - 明确展示不支持的函数、引用形式和降级计算行为。
- [ ] 展示并操作更多 XLSX 富内容。
  - 支持冻结窗格和隐藏行列的实际视图效果。
  - 支持打开或编辑超链接。
  - 逐步支持批注、数据验证和条件格式。
  - 为表格、图表、透视表和不支持的绘图对象提供可见占位或只读说明。
- [ ] 在前端消费工作簿能力和历史状态。
  - 接入后端返回的 `capabilities` 和 `history` 字段。
  - 根据保护状态和复杂特性禁用不允许的编辑按钮。
  - 在操作前展示具体限制原因，而不是仅在失败后显示错误。
  - 撤销历史被截断时向用户展示原因。
- [ ] 统一跨平台导出体验。
  - Web 和移动端提供文件名与 XLSX/CSV 格式选择。
  - 对 CSV 单工作表、仅值导出等有损行为进行确认。
  - 明确 XLSM 原格式保存和格式转换限制。

## P2：工作区与发布能力

- [ ] 完善本地文件管理。
  - 桌面端增加最近文件列表。
  - 本地工作簿支持重命名、复制、搜索和按更新时间展示。
  - 评估文件夹、缩略图和批量管理需求。
- [ ] 为桌面端增加崩溃恢复。
  - 使用原子写入保存恢复快照。
  - 在启动页展示可恢复文档并允许删除快照。
  - 保持与 Web、iOS 和 Android 一致的恢复语义。
- [ ] 评估云端和协作能力。
  - 明确项目是否继续保持纯本地定位。
  - 如需扩展，分别设计云同步、分享、版本历史和实时协作，避免破坏
    当前无服务端工作簿状态的架构约束。
- [ ] 评估并改善大工作簿支持。
  - 记录 64 MiB 输入、每表 25 万行和 200 万密集单元格槽位等现有限制。
  - 在打开文件前提供清晰的限制提示。
  - 通过稀疏数据结构、分块解析和内存基准决定是否安全提高上限。
- [ ] 补齐发布验收和端到端测试。
  - 在桌面和 Web 覆盖打开、编辑、保存、导出、恢复及未保存关闭保护。
  - 在 iOS 和 Android 真机验证文件选择、取消、图片、首次及重复保存、
    导出、恢复、外部链接和关闭保护。
  - 增加键盘、触摸、可访问性和响应式布局回归测试。

## 建议实施顺序

1. 接入工作簿能力和撤销历史反馈。
2. 建立范围选择、键盘导航和剪贴板基础模型。
3. 接入样式渲染并实现样式编辑协议。
4. 增加排序筛选、合并操作和完整工作表管理。
5. 完善公式、查找替换、跨平台导出和文件管理。
6. 根据产品定位推进富内容、超大文件或协作能力。
