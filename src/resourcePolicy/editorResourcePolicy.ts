export const MAX_RESIDENT_REGION_BYTES = 16 * 1024 * 1024;
export const MAX_DOCUMENT_MANIFEST_RESIDENT_BYTES = 16 * 1024 * 1024;
export const MAX_DOCUMENT_PROJECTION_RESIDENT_BYTES = 20 * 1024 * 1024;
export const MAX_REGION_BLOCK_RESIDENT_BYTES = MAX_RESIDENT_REGION_BYTES;
export const MAX_REGION_STAGING_WIRE_BYTES = 32 * 1024 * 1024;

export const MAX_DOCUMENT_WIRE_BYTES = 20 * 1024 * 1024;
export const MAX_REGION_RESPONSE_BYTES = 16 * 1024 * 1024;
export const MAX_CELL_CHANGES_PER_BATCH = 4_096;
export const MAX_CELL_TEXT_BYTES = 4 * 1024 * 1024;
export const MAX_BATCH_TEXT_BYTES = 8 * 1024 * 1024;
export const MAX_SEARCH_QUERY_BYTES = 4_096;
export const SHEET_REGION_TILE_ROWS = 128;
export const SHEET_REGION_TILE_COLUMNS = 32;
