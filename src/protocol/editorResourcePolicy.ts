import {
  MAX_CELL_TEXT_BYTES as PROTOCOL_MAX_CELL_TEXT_BYTES,
  MAX_MUTATION_TEXT_BYTES as PROTOCOL_MAX_MUTATION_TEXT_BYTES,
  MAX_SEARCH_QUERY_BYTES as PROTOCOL_MAX_SEARCH_QUERY_BYTES,
  MAX_SET_CELL_CHANGES,
  MAX_DOCUMENT_RESPONSE_BYTES,
  MAX_SHEET_REGION_RESPONSE_BYTES,
  SHEET_REGION_TILE_COLUMNS as PROTOCOL_SHEET_REGION_TILE_COLUMNS,
  SHEET_REGION_TILE_ROWS as PROTOCOL_SHEET_REGION_TILE_ROWS,
} from '@/types/protocol';

export const MAX_RESIDENT_REGION_BYTES = 16 * 1024 * 1024;
export const MAX_DOCUMENT_MANIFEST_RESIDENT_BYTES = 16 * 1024 * 1024;
export const MAX_DOCUMENT_PROJECTION_RESIDENT_BYTES = 20 * 1024 * 1024;
export const MAX_REGION_BLOCK_RESIDENT_BYTES = MAX_RESIDENT_REGION_BYTES;
export const MAX_DOCUMENT_WIRE_BYTES = MAX_DOCUMENT_RESPONSE_BYTES;
export const MAX_REGION_RESPONSE_BYTES = MAX_SHEET_REGION_RESPONSE_BYTES;
export const MAX_CELL_CHANGES_PER_BATCH = MAX_SET_CELL_CHANGES;
export const MAX_CELL_TEXT_BYTES = PROTOCOL_MAX_CELL_TEXT_BYTES;
export const MAX_BATCH_TEXT_BYTES = PROTOCOL_MAX_MUTATION_TEXT_BYTES;
export const MAX_SEARCH_QUERY_BYTES = PROTOCOL_MAX_SEARCH_QUERY_BYTES;
export const SHEET_REGION_TILE_ROWS = PROTOCOL_SHEET_REGION_TILE_ROWS;
export const SHEET_REGION_TILE_COLUMNS = PROTOCOL_SHEET_REGION_TILE_COLUMNS;
